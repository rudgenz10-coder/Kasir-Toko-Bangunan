<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= title %></title>
  <link href="/css/output.css" rel="stylesheet">
</head>

<body class="bg-gray-100 p-6">
  
  <div class="max-w-lg mx-auto bg-white p-6 rounded shadow">

    <h1 class="text-2xl font-bold mb-4"><%= title %></h1>

    <form action="<%= action %>" method="POST" class="space-y-4">

      <input 
        type="text" 
        name="username" 
        placeholder="Username" 
        value="<%= user.username || '' %>" 
        class="w-full p-2 border rounded" required>

      <input 
        type="text" 
        name="email" 
        placeholder="Email" 
        value="<%= user.email || '' %>" 
        class="w-full p-2 border rounded" required>

      <input 
        type="text" 
        name="password" 
        placeholder="Password" 
        value="<%= user.password || '' %>" 
        class="w-full p-2 border rounded" required>

      <button class="w-full py-2 bg-green-600 text-white rounded">Simpan</button>
    </form>

  </div>

</body>
</html>
